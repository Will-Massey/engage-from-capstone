# Security/Robustness Audit — 2026-07-07

_Code-verified. Result: no CRITICAL, no HIGH. The P0–P2 hardening held: the
~30-route authz matrix found **zero IDOR** (all `:id` lookups tenant-scoped),
zod coverage near-total, no injectable raw queries, no hardcoded secrets,
webhook signatures verified (Stripe/Revolut/SendGrid), uploads solid._

## Known-open items (confirmed, with mitigations)

| Item                 | Status | Notes                                                                                                                                                                                           |
| -------------------- | ------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Email verification   | OPEN   | `emailVerified` column exists, referenced nowhere. Mitigation: public register OFF in prod unless `ALLOW_PUBLIC_REGISTER=true` + rate limited → tier moves NOW→NEXT until public reg is enabled |
| CSRF session-binding | OPEN   | Global token registry (`utils/csrfStore.ts:27-58`); custom-header requirement still defeats classic CSRF. Low-moderate residual                                                                 |
| AML webhook HMAC     | OPEN   | Static header secret w/ `secureCompare`, 503 if unset in prod (`routes/aml.ts:103-116`). No body HMAC/replay protection                                                                         |

## MEDIUM (each a small diff — fix in hardening batch)

- **M1 — Tenant-wide settings mutable by any staff role** (authenticate but no
  `authorize`): `PUT /api/tenants/settings` (tenants settings module),
  email config routes (`routes/email.ts:81-84, 110-113, 426-429, 352-355, 666-669`),
  `services-new.ts:85-88, 152-155, 178-181`. Fix: `authorize('ADMIN','PARTNER','MANAGER')`.
- **M2 — HTML injection into practice notification emails** from
  share-token-holder input: decline/skip `reason` interpolated into email HTML
  unescaped (`routes/proposalsShare/publicSign.ts:472-491, 313-332`). Fix:
  HTML-escape user strings before building `html`.
- **M3 — Staff accept route**: `POST /api/proposals/:id/accept` has no zod and
  no role gate (`routes/proposals/lifecycle.ts:208-213`) while sibling ops
  require SENIOR+. Fix: zod schema (max lengths) + authorize.

## LOW

- **L1** Latent auth bypass in **unmounted** diagnostics route (`undefined !== undefined`
  passes when env unset) — delete or gate (`routes/diagnostics.ts`).
- **L2** Cloudflare email webhook secret compared with `===` not `secureCompare`
  (`routes/webhooks/cloudflare-email.ts:40`).
- **L3** Provider/internal error messages forwarded to clients:
  `routes/aml.ts:64, 94, 145`; `publicSign.ts:252-253`. Log, return generic.
- **L4** Public `/api/status` discloses provider config detail strings (by design; trim).
- **L5** Share/portal tokens are uuidv4-derived (~121 bits, CSPRNG) — fine;
  cosmetic upgrade to `crypto.randomBytes(24).toString('base64url')`.

## Dependency audit

root/backend: 1 low + 1 moderate (`uuid` GHSA-w5hq-g745-h8pq, fix breaking);
frontend 1 low; shared 0. No high/critical.
